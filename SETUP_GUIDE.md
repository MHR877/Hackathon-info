# دليل الإعداد السريع - DzTruth

## الخطوات المطلوبة

### 1. إعداد قاعدة البيانات MongoDB

يجب أن يكون MongoDB مثبتاً ويعمل على جهازك.

**للتحقق من تشغيل MongoDB:**
```bash
mongosh
```

إذا لم يكن مثبتاً، قم بتنزيله من: https://www.mongodb.com/try/download/community

### 2. إعداد ملف البيئة (.env.local)

قم بإنشاء ملف `.env.local` في مجلد `frontend` وأضف المحتوى التالي:

```
MONGODB_URI=mongodb://localhost:27017/dztruth
BACKEND_AI_URL=http://localhost:5000
```

### 3. تشغيل Backend AI Server

افتح نافذة Terminal جديدة:

```bash
cd "c:\Users\meham\OneDrive\Desktop\Hackathon Amin\backend-ai"
python app.py
```

يجب أن يعمل الخادم على: http://localhost:5000

### 4. تشغيل Frontend Server

افتح نافذة Terminal أخرى:

```bash
cd "c:\Users\meham\OneDrive\Desktop\Hackathon Amin\frontend"
npm run dev
```

يجب أن يعمل الموقع على: http://localhost:3000

## الصفحات المتاحة

- **الرئيسية**: http://localhost:3000/
- **السجل**: http://localhost:3000/history
- **حول**: http://localhost:3000/about
- **تواصل معنا**: http://localhost:3000/contact

## حل المشاكل الشائعة

### مشكلة: Port 3000 is in use
**الحل**: أوقف الخادم القديم أو استخدم المنفذ البديل (3001)

### مشكلة: MongoDB connection error
**الحل**: تأكد من تشغيل MongoDB:
```bash
mongod
```

### مشكلة: Backend AI not responding
**الحل**: تأكد من تثبيت المكتبات المطلوبة:
```bash
pip install flask joblib scikit-learn deep-translator
```

## الميزات المتاحة

✅ **الصفحة الرئيسية**
- إدخال نص بالدارجة الجزائرية
- رفع صور تحتوي على نصوص
- التحقق الفوري من صحة الأخبار
- عرض النتائج بتصميم جميل

✅ **صفحة السجل**
- عرض جميع الفحوصات السابقة
- إحصائيات شاملة (المجموع، الصحيح، الزائف)
- فلترة النتائج (الكل، صحيح، زائف)
- البحث في السجلات
- حذف جميع السجلات

✅ **صفحة حول**
- معلومات عن المنصة
- كيفية العمل
- المميزات
- الأسئلة الشائعة (FAQ)

✅ **صفحة التواصل**
- نموذج إرسال رسائل
- حفظ الرسائل في قاعدة البيانات
- رسالة نجاح عند الإرسال

## التصميم

- ✨ تصميم عصري وجذاب
- 🎨 ألوان متدرجة وحيوية
- 📱 متجاوب مع جميع الأجهزة
- 🌙 دعم الوضع الليلي
- ⚡ رسوم متحركة سلسة
- 🔤 خطوط عربية جميلة (Cairo, Tajawal)
- ➡️ دعم كامل للغة العربية (RTL)

## قاعدة البيانات

### Collections في MongoDB:

1. **checks** - سجل الفحوصات
   - text: النص الأصلي
   - image: الصورة (اختياري)
   - prediction: النتيجة (Fake/True)
   - processedText: النص المعالج
   - timestamp: وقت الفحص
   - userIp: IP المستخدم

2. **contacts** - رسائل التواصل
   - name: الاسم
   - email: البريد الإلكتروني
   - message: الرسالة
   - timestamp: وقت الإرسال
   - status: الحالة (new/read/replied)

## API Endpoints

### POST /api/check
```json
{
  "text": "النص المراد فحصه",
  "image": "base64_image_optional"
}
```

### GET /api/history?filter=all&limit=50
```json
{
  "checks": [...],
  "stats": {
    "total": 100,
    "fake": 30,
    "true": 70,
    "suspicious": 0
  }
}
```

### DELETE /api/history
حذف جميع السجلات

### POST /api/contact
```json
{
  "name": "الاسم",
  "email": "email@example.com",
  "message": "الرسالة"
}
```

---

للدعم: contact@dztruth.dz
