# DzTruth - كاشف الأخبار الزائفة بالدارجة الجزائرية

منصة ذكية للتحقق من صحة الأخبار المكتوبة بالدارجة الجزائرية باستخدام الذكاء الاصطناعي.

## المميزات

- ✅ تحليل نصوص بالدارجة الجزائرية
- 📸 رفع صور تحتوي على نصوص
- 📊 إحصائيات وسجل الفحوصات
- 🎨 واجهة عصرية وسهلة الاستخدام
- 🌙 دعم الوضع الليلي
- 📱 تصميم متجاوب لجميع الأجهزة
- 🗄️ تخزين البيانات في MongoDB

## التقنيات المستخدمة

### Frontend
- **Next.js 16** - إطار عمل React
- **TypeScript** - لغة البرمجة
- **Tailwind CSS** - تنسيق الواجهة
- **Lucide React** - الأيقونات

### Backend
- **Flask** - خادم Python
- **scikit-learn** - نماذج التعلم الآلي
- **MongoDB** - قاعدة البيانات

## التثبيت والإعداد

### 1. تثبيت MongoDB

قم بتنزيل وتثبيت MongoDB من [الموقع الرسمي](https://www.mongodb.com/try/download/community)

### 2. إعداد Backend (AI Server)

```bash
cd backend-ai

# تثبيت المكتبات المطلوبة
pip install flask joblib scikit-learn deep-translator

# تشغيل الخادم
python app.py
```

الخادم سيعمل على `http://localhost:5000`

### 3. إعداد Frontend

```bash
cd frontend

# تثبيت المكتبات
npm install

# إنشاء ملف البيئة
# قم بإنشاء ملف .env.local وأضف:
# MONGODB_URI=mongodb://localhost:27017/dztruth
# BACKEND_AI_URL=http://localhost:5000

# تشغيل خادم التطوير
npm run dev
```

الموقع سيعمل على `http://localhost:3000`

## البنية

```
frontend/
├── app/
│   ├── api/
│   │   ├── check/          # API للتحقق من الأخبار
│   │   ├── history/        # API لسجل الفحوصات
│   │   └── contact/        # API لنموذج التواصل
│   ├── about/              # صفحة حول
│   ├── contact/            # صفحة التواصل
│   ├── history/            # صفحة السجل
│   ├── globals.css         # التنسيقات العامة
│   ├── layout.tsx          # التخطيط الرئيسي
│   └── page.tsx            # الصفحة الرئيسية
├── components/
│   ├── Navbar.tsx          # شريط التنقل
│   └── Footer.tsx          # التذييل
└── lib/
    ├── mongodb.ts          # اتصال MongoDB
    └── types.ts            # أنواع TypeScript

backend-ai/
├── app.py                  # خادم Flask
├── processe.py             # معالجة النصوص
├── svm_model.pkl           # نموذج SVM
└── vectorizer.pkl          # Vectorizer
```

## الاستخدام

1. **الصفحة الرئيسية**: أدخل نصاً أو ارفع صورة للتحقق من صحة الخبر
2. **السجل**: عرض جميع الفحوصات السابقة مع إحصائيات
3. **حول**: معلومات عن المنصة وكيفية عملها
4. **تواصل معنا**: إرسال رسائل واستفسارات

## API Endpoints

### POST /api/check
التحقق من صحة نص

```json
{
  "text": "النص المراد التحقق منه",
  "image": "base64_image_optional"
}
```

### GET /api/history
الحصول على سجل الفحوصات

Query Parameters:
- `filter`: all | fake | true
- `limit`: عدد النتائج (افتراضي: 50)

### POST /api/contact
إرسال رسالة تواصل

```json
{
  "name": "الاسم",
  "email": "البريد الإلكتروني",
  "message": "الرسالة"
}
```

## المساهمة

نرحب بجميع المساهمات! يرجى:
1. عمل Fork للمشروع
2. إنشاء فرع جديد للميزة
3. إرسال Pull Request

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للجميع.

## الدعم

للدعم والاستفسارات: contact@dztruth.dz

---

صنع بكل ❤️ في الجزائر
